<h1>All Users</h1>

<ul id="users">
  <% @users.each do |user| %>
    <li class="d-flex">
      <%= link_to user.username, user_path(user),
    class: 'user_title', id: "user_#{user.id}" %>
    </li>

  <% end %>
</ul>


<%= link_to "Create a New User", new_user_path, class: "new_user" %>
